#!/usr/bin/python
### problem to set the number in the classs attrribute
class webber:
 population = 0

   def __init__(self, name):
        self.name = name
        webber.population += 1
        print("my new number is now{webber.population}.")

    def set_raiseAmount(cls, name):
        cls.name = name
        first, last, pay = some_variable.split('-') 

    return cls(first, last, pay) 

    
    
    
    
    
